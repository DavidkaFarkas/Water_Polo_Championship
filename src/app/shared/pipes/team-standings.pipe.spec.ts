import { TeamStandingsPipe } from './team-standings.pipe';

describe('TeamStandingsPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamStandingsPipe();
    expect(pipe).toBeTruthy();
  });
});
